// Includes (trabaja sobre arreglos o strings)
    // Este metodo nos sirve para comprobar el contenido

    let numbers = [1, 2, 3, 7, 8];

    if (numbers.includes(9)) {
        console.log('Sí se encuentra el valor 7');
    } else {
        console.log('No se encuentra.');
    }

// Elavar a la potencia

    let base = 4;
    let exponent = 4;
    let result = base ** exponent;

    console.log(result);

// ECMASCRIPT7 June 2016